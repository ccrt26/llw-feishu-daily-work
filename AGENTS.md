# LLW Personal Assistant 组件维护规则

## 开始前必读

- 先完整阅读工作区 `AGENTS.md`、`.llw-system/README.md`，并按其导航读取
  `.llw-system/SYSTEM_MAP.md`、当前唯一设计稿、当前实施计划以及本任务涉及的
  Skill 和 references。
- 当前生产事实以 `.llw-system/SYSTEM_MAP.md` 为准；未来设计事实以系统地图指向
  的唯一当前设计稿为准。历史设计、旧对话和较旧 `main` 不得覆盖它们。
- 开始实现前确认实际生产分支、提交、配置和运行状态。功能分支必须从项目所有者
  明确批准的生产提交或其后继提交建立，不得默认从较旧的 `main` 开始。

## 实现边界

- 飞书和微信只是入口与出口，共用一个 LLW Personal Assistant、同一组安全工具、
  Writer、Outcome 和恢复逻辑。
- 不新增独立 AI Router、第二个 Agent、Capability registry、公共候选
  normalizer、重复业务 Schema、第二条飞书消费链或每能力一个长期服务。
- Codex 为主模型；不得自动扩大 DeepSeek 的现有批准范围。
- 每轮最多调用一个现有副作用工具。来源读取、PDF 准备、媒体探测和转写只能是
  只读来源准备，不能自行授权 Writer。
- 只修改当前任务明确涉及的组件内容；不得整理、移动、改名或覆盖用户资料。
- 密钥、令牌和平台标识不得写入仓库、工作区、普通日志、测试夹具或模型上下文。
- 未经项目所有者明确授权，不扩大权限、不安装或更新生产运行件、不切换生产、
  不提交或推送 GitHub。

## 子智能体使用

- 默认不使用子智能体。仅当存在两个或以上可以独立完成、边界明确、没有共享可变
  状态的工作流，并且并行委派能够显著降低风险或总耗时时，才可以使用。
- 不得把规则与 Skill 阅读、生产状态核验、安全或权限决策、共享核心修改、最终
  验证委派给子智能体。最终结论必须由主智能体依据原始证据复核。

## 测试与验收

- 修改前先运行与变更范围直接相关的现有测试；新增行为和缺陷修复必须先写一个能
  稳定复现目标问题的失败测试，确认按预期失败后再写最小实现。
- 不得把“通过几百项回归”当作功能正确或问题已解决的充分证据。运行大规模回归前，
  必须先说明它要验证的具体风险、失败时能暴露什么，以及结果是否能够回答当前问题。
- 日常迭代优先运行最小确定性测试和受影响合同测试。用户可见功能必须再通过至少一条
  真实纵向旅程验证其确实可行，旅程应尽量覆盖：
  `入口 → Source Intake → 安全检查 → Personal Assistant → 只读结果或一个工具
  → Writer/零写入证明 → Outcome → 原入口回复`。
- 完整数百项回归只在共享核心形成候选版本、部署/发布门禁，或定向测试不足以排除
  跨能力回退风险时运行一次。不得在每个小改动后机械重复完整回归。
- 真实入口测试和生产验收默认以微信为主。只有变更依赖飞书专有权限、资源下载、
  消息字段或其他无法由共享核心和微信证明的飞书合同，才运行必要的最小飞书测试；
  不得为了入口对称而重复整套飞书验收。
- 飞书、微信真实验收结束后必须明确结束或取消测试 Task，并验证测试来源按合同
  清理，避免来源进入下一次真实任务。
- 部署前必须运行与风险相称的定向测试、一次必要的完整回归、回滚验证和运行健康
  检查；没有新鲜验证证据不得宣称完成、修复或可用。

## 变更与交付

- 遵循严格 TDD：RED（确认测试因缺失行为失败）→ GREEN（最小实现）→
  REFACTOR（保持测试通过）。
- 优先修复根因，不通过单纯增加超时、放宽校验或重复重试掩盖证据缺失。
- Writer、回复、权限、配置和生产切换保持独立门禁。真实 Writer 成功前不得宣称
  已保存、归档、生成、发送或完成。
- 只有真实验收完成后，才更新组件 README、`docs/PROJECT_OVERVIEW.md` 和工作区
  `.llw-system/SYSTEM_MAP.md` 的生产状态。
